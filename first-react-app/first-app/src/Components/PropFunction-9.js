import React from "react";

//props using function component

const PropFunction = (props) =>{
    return <p>Hello I am {props.name} {props.lastName}. and this is props revision for functional component</p>
}

export default PropFunction